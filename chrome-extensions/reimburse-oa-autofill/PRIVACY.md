# 隐私说明

该插件在用户的 Chrome 中本地读取所选 Excel，不向服务器上传文件或解析结果。

插件不使用 `chrome.storage`，不保存 OA 账号、密码、Cookie 或 Excel 内容。关闭弹窗或替换文件后，内存中的文件数据会被丢弃。

插件只申请以下权限：

- `activeTab`：向用户当前打开的 OA 页签发送页面检查和填写指令。
- `https://oa.jugeng.com:8445/*`：只在 OA 域名下运行页面填写代码。

插件不上传附件，不点击保存、批准、退回或提交。
